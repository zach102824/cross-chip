\documentclass[11pt]{article}

% ---- Overleaf-ready preamble -------------------------------------------------
\usepackage[margin=1in]{geometry}
\usepackage{amsmath,amssymb}
\usepackage{bm}
\usepackage{booktabs}
\usepackage{array}
\usepackage{longtable}
\usepackage{xcolor}
\usepackage{tcolorbox}
\usepackage{hyperref}
\hypersetup{colorlinks=true,linkcolor=blue,urlcolor=blue}

% Section titles contain math macros (\bm, \texttt). hyperref turns titles into
% PDF bookmark strings and chokes on \bm's internal #1 ("Illegal parameter number
% in definition of \Hy@tempa"). Neutralize those macros in PDF-string context.
\pdfstringdefDisableCommands{%
  \def\bm#1{#1}%
  \def\boldsymbol#1{#1}%
  \def\texttt#1{#1}%
  \def\mathrm#1{#1}%
}

% Standalone (never-subscripted) macros only, to avoid double-subscript errors.
\newcommand{\Emit}{E_{\mathrm{mit}}}
\newcommand{\Eideal}{E_{\mathrm{ideal}}}
\newcommand{\egs}{e_{\mathrm{gs}}}
\newcommand{\boff}{b_{\mathrm{off}}}

\title{\textbf{The GP-mitigated energy and its gradient, worked out with real numbers\\
(HF molecule, bond $1.2$\,\AA, from \texttt{main\_HF\_ML\_tog.ipynb})}}
\author{Documentation for \texttt{gp\_mitigator\_ML\_tog.py}}
\date{}

\begin{document}
\maketitle

\begin{tcolorbox}[colback=blue!4!white,colframe=blue!50!black,title=\textbf{What this document does}]
It walks through the whole pipeline \textbf{in chronological order} -- the exact
sequence the code runs -- and explains, from scratch, how the \emph{mitigated
energy} $\Emit(\bm\theta)$ and its \emph{gradient} $\nabla\Emit$ are produced,
with \textbf{real numbers} from the first two VQE iterations. The order is:
\textbf{(1)}~generate near-Clifford training data, \textbf{(2)}~encode each row as
a feature vector, \textbf{(3)}~fit one Gaussian process, \textbf{(4)}~predict a
mitigated value, \textbf{(5)}~assemble the energy, \textbf{(6)}~compute the
gradient (including the $\pm\pi/2$ measurements), \textbf{(7)}~step and repeat.
Every number here was produced by \texttt{docs/make\_example\_numbers.py}
(settings: $60$ warm-start circuits, spread $0.8$, $8192$ shots, max-harmonic
$2$, learning rate $0.5$, seed $1234$); the raw dump is in
\texttt{docs/example\_numbers.json}.
\end{tcolorbox}

\section{Symbol glossary: every symbol, with a concrete example}
\label{sec:glossary}

Before any derivation, here is \emph{every} math symbol used in this document.
For each one: what kind of object it is (scalar / vector / matrix and its size),
a concrete example value, and how it is obtained or computed. All example values
are the real numbers from the worked iterations unless marked
``(illustrative)'' (those come from the worked $3$-point GP example in
Section~\ref{sec:worked-gp}). ``Eh'' is Hartree energy units.

{\small
\renewcommand{\arraystretch}{1.35}
\begin{longtable}{@{}p{2.5cm} p{3.4cm} p{3.5cm} p{4.6cm}@{}}
\toprule
\textbf{Symbol} & \textbf{What it is (type \& size)} & \textbf{Concrete example} & \textbf{How obtained / computed} \\
\midrule
\endfirsthead
\toprule
\textbf{Symbol} & \textbf{What it is (type \& size)} & \textbf{Concrete example} & \textbf{How obtained / computed} \\
\midrule
\endhead
\midrule
\multicolumn{4}{@{}r@{}}{\emph{continued on next page}}\\
\endfoot
\bottomrule
\endlastfoot

\multicolumn{4}{@{}l@{}}{\textbf{Problem / Hamiltonian}}\\
\midrule
$H$ & operator (Hermitian matrix, $2^8\!\times\!2^8=256\!\times\!256$) & the HF-molecule energy operator & given by quantum chemistry (fermion-to-qubit mapping); the thing we minimize \\
$I$ & the identity Pauli/operator & \texttt{IIIIIIII} (or the $256\!\times\!256$ identity) & fixed \\
$\boff$ & scalar (one real number) & $-95.435792$ Eh & the constant (identity) term of $H$; \texttt{adapter.offset} \\
$P_i$ & a Pauli string (operator), an $8$-letter word over $\{I,X,Y,Z\}$ & \texttt{IIIIIZII} $=$ a $Z$ on qubit $5$ & the $i$-th term of $H$; there are $104$ of them \\
$i$ & integer index over Paulis, $1\le i\le104$ & $i=1$ (the term \texttt{IIIIIZII}) & enumerates the $104$ terms \\
$w_i$ & scalar (real coefficient of $P_i$) & $w_1=+0.940591$ & Hamiltonian coefficient; \texttt{adapter.weights} \\
$\langle P_i\rangle$ & scalar in $[-1,1]$ (expectation value) & $-1.0$ & $\mathrm{Tr}[\rho P_i]$ for the prepared state \\

\midrule
\multicolumn{4}{@{}l@{}}{\textbf{Ansatz parameters \& energies}}\\
\midrule
$\bm\theta$ & vector, length $3$ (the ansatz angles) & $(0.112001,\,0.015816,\,0.031416)$ & the current VQE iterate (starts at $(0,0,0)$, updated by gradient steps) \\
$\theta_j$ & scalar (the $j$-th angle), $j=1,2,3$ & $\theta_1=0.112001$ & component $j$ of $\bm\theta$ \\
$\egs$ & scalar (exact ground-state energy) & $-98.573878$ Eh & reference from exact diagonalization \\
$\Emit(\bm\theta)$ & scalar (mitigated ``blue'' energy) & $-98.546879$ Eh & $\boff+\sum_i w_i\,O_{\mathrm{mit},i}$, Eq.~\eqref{eq:Emit} \\
$\Eideal(\bm\theta)$ & scalar (exact ``green'' energy) & $-98.546109$ Eh & same sum but with $O_{\mathrm{ideal}}$ \\

\midrule
\multicolumn{4}{@{}l@{}}{\textbf{The three versions of one Pauli's value (all scalars in $[-1,1]$)}}\\
\midrule
$O_{\mathrm{ideal}}(\bm\theta,P_i)$ & scalar (exact / ``green'') & $-0.999875$ & noiseless statevector simulation \\
$O_{\mathrm{noisy}}(\bm\theta,P_i)$ & scalar (measured) & $-0.800333$ & noisy density matrix $+$ $8192$ shots $+$ readout mitigation \\
$O_{\mathrm{mit}}(\bm\theta,P_i)$ & scalar (mitigated / ``blue'') & $-1.001467$ & the GP posterior mean $m(\bm x_*)$ at this point \\

\midrule
\multicolumn{4}{@{}l@{}}{\textbf{Feature vectors (GP inputs)}}\\
\midrule
$\bm x$ & vector, length $D=48$ (one GP input row) & the $48$ numbers in Section~\ref{sec:feature} & \texttt{build\_feature\_row}: $[$angle$\,|\,$noisy$\,|\,$pauli$]$ \\
$\bm x_*$ & vector, length $48$ (the \emph{query} row we want a prediction for) & the iteration-2 row for \texttt{IIIIIZII} & built from the current $(\bm\theta,P_i,O_{\mathrm{noisy}})$ \\
$\bm x_n$ & vector, length $48$ (the $n$-th \emph{training} row) & a near-Clifford row's features & built the same way from a near-Clifford circuit \\
$D$ & integer (feature dimension) & $48$ & $12$ angle $+1$ noisy $+35$ pauli \\
$\bm a$ & vector, length $12$ (angle sub-block of $\bm x$) & $[\cos\theta_1,\sin\theta_1,\dots]$ & first $12$ entries of $\bm x$ \\
$o$ (also $o_*,o_n$) & scalar (the single noisy feature column) & $o_*=-0.80$ (illustrative) & entry $13$ of $\bm x$, equals $O_{\mathrm{noisy}}$ \\
$\bm p$ & vector, length $35$ (Pauli sub-block of $\bm x$) & one-hot $+[\,1,0,1\,]$ summaries & last $35$ entries of $\bm x$ \\

\midrule
\multicolumn{4}{@{}l@{}}{\textbf{Training set \& targets}}\\
\midrule
$N$ & integer (number of training rows) & $1000$ (capped from $6240$) & one row per (near-Clifford circuit, Pauli) \\
$n$ & integer index over training rows, $1\le n\le N$ & $n=1$ & enumerates the rows \\
$y_n$ & scalar (training target $=O_{\mathrm{ideal}}$ of row $n$) & $-1.0$ & noiseless sim of that near-Clifford circuit \\
$\bm y$ & vector, length $N$ & $(y_1,\dots,y_N)$ & stacked targets \\
$\bar y$ & scalar (mean of targets) & $-0.42$ (illustrative) & $\tfrac1N\sum_n y_n$ \\
$\sigma_y$ & scalar (std of targets) & $0.55$ (illustrative) & $\sqrt{\tfrac1N\sum_n(y_n-\bar y)^2}$ \\
$\tilde y_n$, $\tilde{\bm y}$ & scalar / vector (standardized targets) & $\tilde y_n=(y_n-\bar y)/\sigma_y$ & normalization, Eq.~\eqref{eq:normy} \\

\midrule
\multicolumn{4}{@{}l@{}}{\textbf{Gaussian process \& kernel}}\\
\midrule
$k(\bm x,\bm x')$ & scalar (kernel / covariance between two rows) & $k(\bm x_*,\bm x_1)=1.10181$ & Eq.~\eqref{eq:kernel} (sum of three pieces) \\
$K$ & matrix, $N\!\times\!N$ & $K_{mn}=k(\bm x_m,\bm x_n)$ & kernel evaluated on every training pair \\
$\bm k_*$ & vector, length $N$ & $[\bm k_*]_n=k(\bm x_*,\bm x_n)$ & kernel of query vs.\ each training row \\
$I$ (matrix) & $N\!\times\!N$ identity & --- & in $(K+\sigma_w^2 I)$; \emph{not} the Pauli $I$ \\
$\bm\alpha$ & vector, length $N$ (``dual weights'') & $(-0.7431,-0.2000,-0.2000)$ (illustrative, $N{=}3$) & solve $(K+\sigma_w^2 I)\bm\alpha=\tilde{\bm y}$ once at fit time \\
$\alpha_n$ & scalar ($n$-th dual weight) & $\alpha_1=-0.7431$ & component $n$ of $\bm\alpha$ \\
$m(\bm x_*)$ & scalar (GP posterior mean $=O_{\mathrm{mit}}$) & $-1.00149$ (illustrative) & $\bar y+\sigma_y\sum_n\alpha_n k(\bm x_*,\bm x_n)$, Eq.~\eqref{eq:mean} \\
$\sigma^2(\bm x_*)$ & scalar (predictive variance) & small near training data & $k(\bm x_*,\bm x_*)-\bm k_*^{\!\top}(K+\sigma_w^2 I)^{-1}\bm k_*$ \\
$c_1,c_2$ & scalars (kernel amplitudes) & $c_1=0.50,\ c_2=0.80$ (illustrative) & fitted by marginal likelihood \\
$\sigma_0$ & scalar (DotProduct offset) & $0.30$ (illustrative) & fitted; sets the CDR intercept \\
$\sigma_w^2$ & scalar (white-noise variance) & $10^{-3}$ (illustrative) & fitted; the shot-noise level \\
$\bm\ell$, $\ell_d$ & vector / scalar (Mat\'ern length scales, one per column, ARD) & $\ell=2.0$ (illustrative) & fitted; large $\Rightarrow$ that feature matters less \\
$r$ & scalar ($\ge0$, scaled distance between two rows) & $0.30$ (illustrative) & $\|(\bm u-\bm u')\oslash\bm\ell\|_2$, Eq.~\eqref{eq:rdist} \\
$\nu$ & scalar (Mat\'ern smoothness) & $2.5$ & fixed config (\texttt{matern\_nu}) \\
$\delta_{\bm x,\bm x'}$ & scalar ($1$ or $0$, Kronecker delta) & $1$ if same row, else $0$ & equals $1$ only when $\bm x'$ is exactly $\bm x$ \\
$\bm\Theta$ & vector (all kernel hyperparameters) & $(c_1,\sigma_0,\bm\ell^{\mathrm{obs}},c_2,\bm\ell^{\mathrm{base}},\sigma_w^2)$ & chosen to maximize Eq.~\eqref{eq:lml} \\

\midrule
\multicolumn{4}{@{}l@{}}{\textbf{Gradient quantities}}\\
\midrule
$\nabla\Emit$ & vector, length $3$ (energy gradient) & $(-0.224003,-0.031632,-0.062832)$ & chain rule, Eq.~\eqref{eq:grad} \\
$\dfrac{\partial\Emit}{\partial\theta_j}$ & scalar (one gradient component) & $-0.224003$ (for $j{=}1$) & component $j$ of $\nabla\Emit$ \\
$\dfrac{\partial m}{\partial\bm x_*}$ & vector, length $48$ (mean sensitivity to features) & $[\,0.035034,-0.014928,\dots]$ on the $\theta_1$ block & analytic, Eq.~\eqref{eq:dmdx} (classical) \\
$\dfrac{\partial x_d}{\partial\theta_j}$ & scalar (feature-vs-angle Jacobian entry) & $\partial\sin\theta_1/\partial\theta_1=\cos\theta_1=1$ at $\theta{=}0$ & closed form for the Fourier features \\
$\dfrac{\partial O_{\mathrm{noisy},i}}{\partial\theta_j}$ & scalar (device-supplied slope) & $+0.005969$ & parameter shift: $\tfrac12[O_{\mathrm{noisy}}(\!+\tfrac\pi2)-O_{\mathrm{noisy}}(\!-\tfrac\pi2)]$ \\
learning rate & scalar & $0.5$ & fixed; step is $\bm\theta\leftarrow\bm\theta-0.5\,\nabla\Emit$ \\

\midrule
\multicolumn{4}{@{}l@{}}{\textbf{Notation / operators}}\\
\midrule
$\langle\,\cdot\,\rangle$ & expectation value of an operator & $\langle P_i\rangle=-1.0$ & $\mathrm{Tr}[\rho\,\cdot\,]$ \\
$\oslash$ & element-wise (Hadamard) division & $(2,4)\oslash(1,2)=(2,2)$ & divide vectors entry-by-entry \\
$\Vert$, $\oplus$ & concatenation of two vectors & $\bm a\Vert\bm p$ stacks the $12$ angle and $35$ pauli entries & glue feature sub-blocks end to end \\
$\|\cdot\|_2$ & Euclidean (L2) norm & $\|(3,4)\|_2=5$ & $\sqrt{\sum_d(\cdot)_d^2}$ \\
$(\cdot)^{\!\top}$ & transpose & turns a column into a row & matrix/vector transpose \\
$\mathrm{Tr}[\cdot]$ & trace of a matrix & sum of diagonal entries & used for expectation values \\
\end{longtable}
}

\section{Setup and notation}

The molecule is HF at bond $1.2$\,\AA. Its Hamiltonian is a constant plus a
weighted sum of $104$ Pauli strings on $8$ qubits:
\begin{equation}
H \;=\; \boff\,I \;+\; \sum_{i=1}^{104} w_i\, P_i ,
\qquad \boff = -95.435792~\text{Eh (the identity coefficient).}
\label{eq:H}
\end{equation}
Each $P_i$ is an $8$-letter word over $\{I,X,Y,Z\}$ (e.g.\ \texttt{IIIIIZII} = a
single $Z$ on qubit $5$), and $w_i$ is its real coefficient. For ansatz
parameters $\bm\theta=(\theta_1,\theta_2,\theta_3)$ the energy is the weighted
sum of the Pauli expectation values $\langle P_i\rangle$.

For every $(\bm\theta,P_i)$ there are \textbf{three} versions of $\langle P_i\rangle$:

\begin{center}
\renewcommand{\arraystretch}{1.2}
\begin{tabular}{@{}lll@{}}
\toprule
Symbol & Name & How it is obtained \\
\midrule
$O_{\mathrm{ideal}}(\bm\theta,P_i)$ & exact / ``green'' & noiseless statevector simulation (the truth) \\
$O_{\mathrm{noisy}}(\bm\theta,P_i)$ & measured & noisy density matrix + $8192$ shots + readout mitigation (REM) \\
$O_{\mathrm{mit}}(\bm\theta,P_i)$ & mitigated / ``blue'' & one Gaussian Process maps features $\to$ estimate of $O_{\mathrm{ideal}}$ \\
\bottomrule
\end{tabular}
\end{center}

\noindent
The reference exact ground-state energy is $\egs=-98.573878$~Eh. Because the GP
is \emph{trained to predict} $O_{\mathrm{ideal}}$, the blue energy is an estimate
of the green energy: they target the same quantity.

\section{The pipeline in chronological order (roadmap)}
\label{sec:roadmap}

Everything below happens in this exact order. Steps 1--3 are done \emph{once},
\emph{before} the optimizer starts (``warm start''); Steps 4--7 repeat every VQE
iteration. Each step is one section of this document.

\begin{center}
\renewcommand{\arraystretch}{1.3}
\begin{tabular}{@{}c p{3.0cm} p{9.2cm}@{}}
\toprule
\# & \textbf{Step} & \textbf{What happens (and where in this doc)} \\
\midrule
\multicolumn{3}{@{}l@{}}{\emph{Offline, once (warm start)}}\\
\midrule
1 & Make training data & Build $60$ \emph{near-Clifford} circuits around the
start angle, run each on the noisy device \emph{and} on a noiseless simulator, and
record one row per (circuit, Pauli): $60\times104=6240$ rows of
$(\bm\theta,P_i,O_{\mathrm{noisy}},O_{\mathrm{ideal}})$. (Section~\ref{sec:step1}) \\
2 & Encode features & Turn each row's $(\bm\theta,P_i,O_{\mathrm{noisy}})$ into a
$48$-number vector $\bm x$. (Section~\ref{sec:feature}) \\
3 & Fit the GP & On the $48$-dim inputs and targets
$y=O_{\mathrm{ideal}}$: normalize targets, build the kernel, fit $\sim\!86$ kernel
hyperparameters by maximizing the marginal likelihood, cache the dual weights
$\bm\alpha$. (Sections~\ref{sec:gp}--\ref{sec:fit}) \\
\midrule
\multicolumn{3}{@{}l@{}}{\emph{Online, every VQE iteration, at the current $\bm\theta$}}\\
\midrule
4 & Measure \& predict & Run the noisy circuit at $\bm\theta$ to get
$O_{\mathrm{noisy}}(\bm\theta,P_i)$ for all $104$ Paulis; build their feature rows;
the GP returns $O_{\mathrm{mit}}=m(\bm x_*)$. (Section~\ref{sec:worked-gp}) \\
5 & Assemble energy & $\Emit=\boff+\sum_i w_i\,O_{\mathrm{mit},i}$.
(Section~\ref{sec:energy}) \\
6 & Gradient \& step & Run $2$ extra noisy circuits per angle at
$\bm\theta\pm\tfrac\pi2\bm e_j$ to get $\partial O_{\mathrm{noisy}}/\partial\theta_j$,
chain with the analytic GP gradient to get $\nabla\Emit$, and update
$\bm\theta\leftarrow\bm\theta-0.5\,\nabla\Emit$. (Section~\ref{sec:grad}) \\
7 & Top-up \& repeat & If the GP is too uncertain, add local rows and refit; then
go back to Step 4 at the new $\bm\theta$. (Section~\ref{sec:reading}) \\
\bottomrule
\end{tabular}
\end{center}

\section{Step 1 --- Generate the near-Clifford training data}
\label{sec:step1}

The GP can only learn the noise$\to$ideal map from \emph{examples} where we know
both numbers. We manufacture those examples from \textbf{near-Clifford} circuits,
because a near-Clifford circuit's ideal expectation values are cheap to compute
exactly on a classical simulator, yet running it on the noisy device shows how the
hardware distorts them. (\texttt{generate\_near\_clifford} +
\texttt{collect\_rows} in the adapter.)

\subsection{How one near-Clifford circuit is built}
We start from $\bm\theta_{\text{init}}=(0,0,0)$ (the HF reference). For each of the
$60$ circuits, the $3$ ansatz angles are set as follows
(\texttt{n\_nonclifford\_gates}$=2$, \texttt{warmstart\_spread}$=0.8$):
\begin{itemize}
  \item \textbf{$2$ of the $3$ angles} are left ``non-Clifford'': jittered
        randomly within $\pm0.8$ rad of $\bm\theta_{\text{init}}$, i.e.\
        $\theta_j=\theta_{\text{init},j}+\mathrm{Uniform}(-0.8,0.8)$.
  \item \textbf{the remaining angle} is \emph{snapped to the nearest Clifford
        grid} (a multiple of $\pi/2$), which keeps the circuit close to a
        stabilizer circuit so its ideal values are well behaved and span the
        $\{-1,0,+1\}$ corners.
\end{itemize}
This ``mostly Clifford, slightly off'' design is what makes the data
information-rich \emph{and} classically tractable.

\subsection{What is measured for each circuit}
For every circuit the adapter produces, \emph{for all $104$ Paulis at once}:
\begin{itemize}
  \item $O_{\mathrm{ideal}}(\bm\theta,P_i)=\langle P_i\rangle$ from a
        \emph{noiseless} statevector simulation -- this is the regression
        \textbf{target} $y$.
  \item $O_{\mathrm{noisy}}(\bm\theta,P_i)$ from a \emph{noisy} density-matrix
        simulation sampled with $8192$ shots and readout-error mitigation -- this
        becomes a \textbf{feature}.
\end{itemize}

\subsection{The resulting training table}
Each (circuit, Pauli) pair is one row:
\begin{equation}
\text{row }n:\quad \big(\underbrace{\bm\theta_n}_{\text{3 angles}},\ \underbrace{P_i}_{\text{Pauli label}},\ \underbrace{O_{\mathrm{noisy}}(\bm\theta_n,P_i)}_{\text{measured, a feature}},\ \underbrace{O_{\mathrm{ideal}}(\bm\theta_n,P_i)}_{\text{target }y_n}\big).
\end{equation}
With $60$ circuits and $104$ Paulis that is $\mathbf{6240}$ rows. Before the exact
fit (Step~3) these are randomly subsampled to
$\texttt{max\_gp\_train\_points}=\mathbf{1000}$ rows, because the GP fit factorizes
an $N\times N$ matrix and $N=1000$ keeps that fast. A handful of example rows:
\begin{center}
\renewcommand{\arraystretch}{1.2}
\begin{tabular}{@{}cccrr@{}}
\toprule
$n$ & $\bm\theta_n$ (3 angles) & $P_i$ & $O_{\mathrm{noisy}}$ (feature) & $O_{\mathrm{ideal}}$ (target $y_n$) \\
\midrule
1 & $(1.57,\,0.42,\,0.00)$ & \texttt{IIIIIZII} & $-0.61$ & $-1.00$ \\
2 & $(1.57,\,0.42,\,0.00)$ & \texttt{IIIIZIII} & $\phantom{-}0.05$ & $\phantom{-}0.00$ \\
3 & $(0.00,\,-0.33,\,1.57)$ & \texttt{ZIIIIIII} & $-0.72$ & $-1.00$ \\
$\vdots$ & $\vdots$ & $\vdots$ & $\vdots$ & $\vdots$ \\
\bottomrule
\end{tabular}
\end{center}
(Values illustrative; the real rows are in the live run.) \textbf{Key point:} the
device is only ever queried here and in Steps~4/6 -- the GP itself never touches
the quantum computer.

\section{Step 2 --- Encode each row as a 48-number feature vector \texorpdfstring{$\bm x$}{x}}
\label{sec:feature}

Step~3 needs fixed-length numeric inputs, so each row's
$(\bm\theta,P_i,O_{\mathrm{noisy}})$ is turned into a vector $\bm x\in\mathbb R^{48}$
by \texttt{build\_feature\_row}. (The target $y=O_{\mathrm{ideal}}$ is kept
separately; it is what we predict, not an input.) The same encoding is applied to
all $6240$ training rows now, and later to every query in Step~4. It is the
concatenation of three blocks; with the notebook settings ($n_{\text{params}}=3$,
max harmonic $H=2$, $n_{\text{qubits}}=8$):

\begin{center}
\renewcommand{\arraystretch}{1.25}
\begin{tabular}{@{}p{2.0cm} p{8.2cm} c@{}}
\toprule
\textbf{Block} & \textbf{What the numbers are} & \textbf{\# cols} \\
\midrule
angle &
$[\cos\theta_j,\ \sin\theta_j,\ \cos 2\theta_j,\ \sin 2\theta_j]$ for $j=1,2,3$
(Fourier features; make the GP periodic in $\theta$) & $3\!\times\!4=12$ \\
noisy & the single measured value $O_{\mathrm{noisy}}(\bm\theta,P_i)$ & $1$ \\
pauli & per-qubit one-hot over $\{I,X,Y,Z\}$ ($8\!\times\!4=32$) then summaries
$[\,\text{weight},\,n_{XY},\,n_Z\,]$ & $35$ \\
\midrule
\multicolumn{2}{r}{\textbf{Total } $D$} & $\bm{48}$ \\
\bottomrule
\end{tabular}
\end{center}

\subsection*{Worked feature row (real data, iteration 2)}
Take the real iteration-2 point $\bm\theta=(0.112001,\,0.015816,\,0.031416)$ and
Pauli $P=\texttt{IIIIIZII}$ (a $Z$ on qubit $5$), whose measured value there is
$O_{\mathrm{noisy}}=-0.800333$.

\paragraph{Angle block (12 numbers).}
\[
\begin{aligned}
\theta_1=0.112001:&\ \ \cos=0.993734,\ \sin=0.111767,\ \cos2\theta=0.975016,\ \sin2\theta=0.222134,\\
\theta_2=0.015816:&\ \ \cos=0.999875,\ \sin=0.015815,\ \cos2\theta=0.999500,\ \sin2\theta=0.031627,\\
\theta_3=0.031416:&\ \ \cos=0.999507,\ \sin=0.031411,\ \cos2\theta=0.998027,\ \sin2\theta=0.062791.
\end{aligned}
\]
\paragraph{Noisy block (1 number).} $-0.800333$.
\paragraph{Pauli block (35 numbers).} \texttt{IIIIIZII} has $Z$ on qubit $5$, $I$
elsewhere. One-hot in the order $[I,X,Y,Z]$:
\[
\underbrace{[1,0,0,0]}_{q_0}\,
\underbrace{[1,0,0,0]}_{q_1}\,
\underbrace{[1,0,0,0]}_{q_2}\,
\underbrace{[1,0,0,0]}_{q_3}\,
\underbrace{[1,0,0,0]}_{q_4}\,
\underbrace{[0,0,0,1]}_{q_5=Z}\,
\underbrace{[1,0,0,0]}_{q_6}\,
\underbrace{[1,0,0,0]}_{q_7}
\]
followed by summaries $[\,\text{weight}=1,\ n_{XY}=0,\ n_Z=1\,]$. The full $48$-vector is
\[
\bm x=\big[\,
\underbrace{0.993734,\,0.111767,\,\ldots,\,0.062791}_{12\text{ angle}},\ 
\underbrace{-0.800333}_{\text{noisy}},\ 
\underbrace{1,0,0,0,\ \ldots,\ 0,0,0,1,\ \ldots,\ 1,0,0,0}_{32\text{ one-hot}},\ 
\underbrace{1,0,1}_{\text{summaries}}
\,\big].
\]

\section{Step 3 --- Fit one Gaussian process to all rows}
\label{sec:gp}

This is done once, after Steps 1--2. It is self-contained: it states everything
needed to turn the $1000$ training rows into the prediction function
$O_{\mathrm{mit}}(\bm x_*)$, with no prior GP knowledge assumed. Fitting means
three concrete actions: \textbf{(3a)} normalize the targets, \textbf{(3b)} choose
the kernel and optimize its hyperparameters, \textbf{(3c)} cache the dual-weight
vector $\bm\alpha$. After that the GP is frozen and every later prediction is a
cheap weighted sum.

\subsection{Data and the modelling assumption}
We have $N$ training rows. Row $n$ is a feature vector $\bm x_n\in\mathbb{R}^{D}$
(here $D=48$, built exactly as in Section~\ref{sec:feature}) and a scalar target
$y_n=O_{\mathrm{ideal}}(\bm x_n)$ -- the \emph{noiseless} expectation value we
wish we could read off the device. In this pipeline the rows come from
near-Clifford circuits: $60$ circuits $\times\,104$ Paulis $=6240$ candidate
rows, randomly capped at $\texttt{max\_gp\_train\_points}=1000$ before the exact
fit (the cap keeps the $N\times N$ algebra below tractable).

A Gaussian process assumes the unknown function $f(\bm x)$ (the map
features~$\to O_{\mathrm{ideal}}$) is drawn from a Gaussian prior fully described
by a \emph{covariance} (``kernel'') function $k(\bm x,\bm x')$: the closer two
feature vectors are under $k$, the more strongly their function values are
believed to move together. The single modelling choice is therefore the kernel
$k$ (Section~\ref{sec:kernel}). Observation noise (shot noise) is modelled as
i.i.d.\ Gaussian with variance $\sigma_w^2$.

\subsection{Target normalization (what \texttt{normalize\_y} does)}
\label{sec:normalize}
Before fitting, scikit-learn standardizes the targets (the code sets
\texttt{normalize\_y=True}). Let
\begin{equation}
\bar y=\frac1N\sum_{n=1}^N y_n,\qquad
\sigma_y=\sqrt{\frac1N\sum_{n=1}^N (y_n-\bar y)^2},
\qquad
\tilde y_n=\frac{y_n-\bar y}{\sigma_y}.
\label{eq:normy}
\end{equation}
The GP is fit on the dimensionless $\tilde y_n$ and predictions are mapped back
with $y=\bar y+\sigma_y\,\tilde y$. This matters numerically (kernels assume
order-one targets) and it is the reason the constants $\bar y,\sigma_y$ appear in
the prediction formula below.

\subsection{The posterior mean (the only formula we evaluate)}
Given the kernel, define the $N\times N$ training kernel matrix and the
query-to-training kernel vector,
\begin{equation}
K_{mn}=k(\bm x_m,\bm x_n),\qquad
\big[\bm k_*\big]_n=k(\bm x_*,\bm x_n).
\end{equation}
The GP posterior mean at a new point $\bm x_*$ (the mitigated value) is
\begin{equation}
\boxed{\;O_{\mathrm{mit}}(\bm x_*)=m(\bm x_*)
=\bar y+\sigma_y\sum_{n=1}^{N}\alpha_n\,k(\bm x_*,\bm x_n)\;}
\qquad
\bm\alpha=\big(K+\sigma_w^2 I\big)^{-1}\,\tilde{\bm y},
\label{eq:mean}
\end{equation}
where $\tilde{\bm y}=(\bm y-\bar y)/\sigma_y$ are the standardized targets from
\eqref{eq:normy} and $\sigma_w^2$ is the (learned) \texttt{WhiteKernel} noise
level. The vector $\bm\alpha$ -- the ``dual weights'' -- is computed \emph{once}
at fit time (sklearn uses a Cholesky factorization $K+\sigma_w^2 I=LL^{\!\top}$
and solves $L L^{\!\top}\bm\alpha=\tilde{\bm y}$). After that, predicting at any
$\bm x_*$ is just the weighted sum of kernel similarities in \eqref{eq:mean}. The
predictive variance (used by the top-up gate) is
$\sigma^2(\bm x_*)=k(\bm x_*,\bm x_*)-\bm k_*^{\!\top}(K+\sigma_w^2 I)^{-1}\bm
k_*$, but only the mean enters the energy.

\subsection{Intuition}
Equation~\eqref{eq:mean} says: \emph{the mitigated value is a baseline $\bar y$
plus a blend of the training targets, where each training row $n$ is weighted by
how similar the query is to it} ($k(\bm x_*,\bm x_n)$) \emph{times a fixed
coefficient} $\alpha_n$. If $\bm x_*$ is far (under $k$) from every training row,
all $k(\bm x_*,\bm x_n)\to0$ and $m(\bm x_*)\to\bar y$: the GP ``reverts to the
mean.'' This mean-reversion is the origin of the small on-path bias discussed in
Section~\ref{sec:reading}.

\section{Step 3, detail --- the kernel (what ``similarity'' means)}
\label{sec:kernel}

The kernel is assembled in \texttt{SingleGPModel.build\_kernel}. It is a
\emph{sum of three pieces}, evaluated on disjoint groups of the $48$ feature
columns (recall the index map: angle $=$ cols $0$--$11$, noisy $=$ col $12$,
pauli $=$ cols $13$--$47$):
\begin{equation}
\boxed{\,k(\bm x,\bm x')\;=\;\underbrace{k_{\mathrm{lin}}(\bm x,\bm x')}_{\text{CDR line}}
\;+\;\underbrace{k_{\mathrm{base}}(\bm x,\bm x')}_{\text{coherent residual}}
\;+\;\underbrace{\sigma_w^2\,\delta_{\bm x,\bm x'}}_{\text{shot noise}}\,}
\label{eq:kernel}
\end{equation}
with
\begin{align}
k_{\mathrm{lin}}(\bm x,\bm x')&=c_1\,
\underbrace{\big(\sigma_0^2+o\,o'\big)}_{\text{DotProduct on }O_{\mathrm{noisy}}}\;
\underbrace{k^{\nu}_{\mathrm{obs}}\!\big(\bm p,\bm p'\big)}_{\text{Mat\'ern on Pauli cols}},
\label{eq:klin}\\[2pt]
k_{\mathrm{base}}(\bm x,\bm x')&=c_2\,
\underbrace{k^{\nu}_{\mathrm{base}}\!\big(\bm a\Vert\bm p,\ \bm a'\Vert\bm p'\big)}_{\text{Mat\'ern on angle\,$\oplus$\,Pauli cols}} ,
\label{eq:kbase}
\end{align}
where $o=O_{\mathrm{noisy}}$ is the single noisy column, $\bm a$ the $12$ angle
columns, $\bm p$ the $35$ Pauli columns, $\bm a\Vert\bm p$ their concatenation,
and $\delta_{\bm x,\bm x'}=1$ only when $\bm x'$ is \emph{exactly} the same row as
$\bm x$ (so the white term affects the diagonal of $K$ but never the
cross-covariance $\bm k_*$ to a genuinely new query). The amplitudes
$c_1,c_2$, the offset $\sigma_0$, the noise $\sigma_w^2$, and the Mat\'ern length
scales are the \emph{hyperparameters} fit in Section~\ref{sec:fit}.

\subsection{The five leaf kernels, defined}
Each box in \eqref{eq:kernel}--\eqref{eq:kbase} is built from these standard
scikit-learn pieces.

\paragraph{ConstantKernel} $k(\bm x,\bm x')=c$ for all inputs. Used as a scalar
amplitude ($c_1,c_2$); it scales whatever it multiplies.

\paragraph{DotProduct} on the single noisy column,
\begin{equation}
k_{\mathrm{dot}}(o,o')=\sigma_0^2+o\,o'.
\end{equation}
This is the non-stationary piece that, after the linear-algebra of
\eqref{eq:mean}, reproduces an affine map $a\cdot o+b$ from the measured value to
the prediction -- i.e.\ the classic CDR line $O_{\mathrm{ideal}}\approx a\,
O_{\mathrm{noisy}}+b$, with the slope $a$ set by $c_1$ and the intercept by
$\sigma_0$. ``CDR is absorbed in the kernel'' means exactly this term.

\paragraph{Mat\'ern} ($\nu=2.5$, the default) on a feature group with ARD
(one length scale $\ell_d$ per active column). With the scaled distance
\begin{equation}
r=\Big\|\,(\bm u-\bm u')\oslash\bm\ell\,\Big\|_2
=\sqrt{\sum_{d}\Big(\frac{u_d-u'_d}{\ell_d}\Big)^2},
\label{eq:rdist}
\end{equation}
the Mat\'ern-$\tfrac52$ covariance is
\begin{equation}
\boxed{\;k^{5/2}(r)=\Big(1+\sqrt5\,r+\tfrac{5}{3}r^2\Big)\,e^{-\sqrt5\,r}\;}
\label{eq:matern25}
\end{equation}
which equals $1$ at $r=0$ (identical points) and decays smoothly to $0$ as the
points separate. (For reference the code also supports
$k^{1/2}(r)=e^{-r}$, $k^{3/2}(r)=(1+\sqrt3\,r)e^{-\sqrt3\,r}$, and the
RBF/Gaussian limit $k_{\mathrm{RBF}}(r)=e^{-r^2/2}$ if \texttt{kernel\_type=rbf};
$\nu=2.5$ is what runs here.) ARD means each feature has its \emph{own}
length scale, so the optimizer can learn that, say, the $\theta_1$ cosine feature
matters more than a particular one-hot Pauli bit.

\paragraph{WhiteKernel} $k(\bm x,\bm x')=\sigma_w^2\,\delta_{\bm x,\bm x'}$. It
models i.i.d.\ shot noise on the targets, padding the diagonal of $K$ so the
inverse in \eqref{eq:mean} is well conditioned. Its derivative w.r.t.\ a new
query is zero, so it never affects $m(\bm x_*)$ for a fresh point.

\subsection{How the pieces are wired (\texttt{\_SubsetKernel}, Product, Sum)}
Vanilla scikit-learn applies every kernel to \emph{all} columns. To restrict a
kernel to a column group the code wraps it in \texttt{\_SubsetKernel(inner,
indices)}, which simply slices the columns before calling \texttt{inner}. The
composite is then built with operator overloading:
\begin{itemize}
\item \textbf{Product} ($k_1\!\cdot\!k_2$): used inside $k_{\mathrm{lin}}$ to
      multiply the DotProduct (on the noisy col) by the Mat\'ern $k_{\mathrm{obs}}$
      (on the Pauli cols). Multiplying means ``similar \emph{and}'': two rows are
      linearly coupled \emph{only} when they are also the same/similar Pauli --
      i.e.\ a per-observable CDR slope.
\item \textbf{Sum} ($k_1\!+\!k_2$): used at the top level
      (Eq.~\eqref{eq:kernel}) to add the CDR line, the coherent residual, and the
      white noise. Summing means the contributions are independent additive
      channels of covariance.
\end{itemize}
So in code, \eqref{eq:kernel} reads
\[
\texttt{kernel}=\underbrace{C(c_1)\!\cdot\!\mathrm{Dot}(\sigma_0)|_{\text{noisy}}\!\cdot\!\mathrm{Matern}|_{\text{pauli}}}_{k_{\mathrm{lin}}}
\;+\;\underbrace{C(c_2)\!\cdot\!\mathrm{Matern}|_{\text{angle}\oplus\text{pauli}}}_{k_{\mathrm{base}}}
\;+\;\mathrm{White}(\sigma_w^2).
\]

\subsection{Step 3b --- how many hyperparameters, and how they are fit}
\label{sec:fit}

\paragraph{Exactly how many free numbers?} With ARD (one length scale per active
column) the kernel \eqref{eq:kernel} has the following tunable hyperparameters:
\begin{center}
\renewcommand{\arraystretch}{1.2}
\begin{tabular}{@{}lcl@{}}
\toprule
Hyperparameter & Count & Where \\
\midrule
$c_1$ (amplitude of $k_{\mathrm{lin}}$) & $1$ & ConstantKernel \\
$\sigma_0$ (DotProduct offset) & $1$ & in-kernel CDR intercept \\
$\bm\ell^{\mathrm{obs}}$ (Mat\'ern length scales on the Pauli cols) & $35$ & ARD over the $35$ pauli features \\
$c_2$ (amplitude of $k_{\mathrm{base}}$) & $1$ & ConstantKernel \\
$\bm\ell^{\mathrm{base}}$ (Mat\'ern length scales on angle$\oplus$pauli) & $47$ & ARD over $12+35$ features \\
$\sigma_w^2$ (white-noise level) & $1$ & WhiteKernel \\
\midrule
\textbf{Total} & $\mathbf{86}$ & \\
\bottomrule
\end{tabular}
\end{center}
These $86$ numbers (collected as the vector $\bm\Theta$) are chosen to maximize
the log marginal likelihood of the training targets,
\begin{equation}
\log p(\tilde{\bm y}\mid X,\bm\Theta)=
-\tfrac12\,\tilde{\bm y}^{\!\top}\big(K_{\bm\Theta}+\sigma_w^2 I\big)^{-1}\tilde{\bm y}
-\tfrac12\log\big|K_{\bm\Theta}+\sigma_w^2 I\big|
-\tfrac{N}{2}\log 2\pi,
\label{eq:lml}
\end{equation}
the first term rewarding fit and the second penalizing model complexity
(Occam balance). scikit-learn runs L-BFGS with
\texttt{n\_restarts\_optimizer}$=2$ from random initializations and keeps the best
$\bm\Theta$; bounds are $\ell\in[10^{-3},10^{3}]$,
$\sigma_0\in[10^{-5},10^{2}]$, $\sigma_w^2\in[10^{-8},10^{2}]$.

\paragraph{Step 3c --- cache the dual weights.} With $\bm\Theta$ fixed, build the
$1000\times1000$ matrix $K$, add $\sigma_w^2 I$, Cholesky-factorize once, and solve
\begin{equation}
\bm\alpha=(K+\sigma_w^2 I)^{-1}\tilde{\bm y}\quad(\text{length-}1000\text{ vector}).
\end{equation}
The GP is now frozen: the training inputs $\{\bm x_n\}$, the weights $\bm\alpha$,
the constants $\bar y,\sigma_y$, and $\bm\Theta$ are all stored, and \emph{no
further training or device calls} are needed to predict. Predicting (Step~4) is
just Eq.~\eqref{eq:mean}.

\section{Step 4 --- Predict one mitigated value (worked example)}
\label{sec:worked-gp}

Now the loop starts. At the current $\bm\theta$ we run the noisy circuit once,
read $O_{\mathrm{noisy}}(\bm\theta,P_i)$ for all $104$ Paulis, build each query
feature row $\bm x_*$ (Step~2 encoding), and feed it to the frozen GP. ``Predict''
means evaluate \eqref{eq:mean} -- a weighted sum of similarities to the stored
training rows. To keep the arithmetic on paper, this
example uses a \textbf{three-point training set} and \textbf{illustrative fitted
hyperparameters}; the kernel formulas, the chaining, and every digit of the
arithmetic are exactly what the code does for the real $N{=}1000$-row fit. The
query is the real iteration-2 point for $P=\texttt{IIIIIZII}$, whose measured
value is $o_*=O_{\mathrm{noisy}}=-0.80$.

\paragraph{Illustrative fitted hyperparameters.}
\[
c_1=0.50,\quad \sigma_0=0.30\ (\sigma_0^2=0.09),\quad c_2=0.80,\quad
\ell=2.0\ (\text{shared, for legibility}),\quad \sigma_w^2=10^{-3},
\]
and target normalization constants $\bar y=-0.42$, $\sigma_y=0.55$.

\paragraph{Three training rows.} Each has a measured value $o_n$, a Pauli-block
distance $r^{\mathrm{obs}}_n$ to the query (0 if same Pauli), and an
angle$\oplus$Pauli distance $r^{\mathrm{base}}_n$:
\begin{center}
\renewcommand{\arraystretch}{1.2}
\begin{tabular}{@{}cccc@{}}
\toprule
$n$ & $o_n$ & $r^{\mathrm{obs}}_n$ & $r^{\mathrm{base}}_n$ \\
\midrule
1 & $-0.78$ & $0.0$ & $0.30$ \\
2 & $-0.62$ & $0.0$ & $0.95$ \\
3 & $-0.85$ & $1.4$ & $1.20$ \\
\bottomrule
\end{tabular}
\end{center}

\paragraph{Step 1 -- Mat\'ern values} via \eqref{eq:matern25}, $\sqrt5=2.236068$.
For the base term ($k^{\nu}_{\mathrm{base}}$), using $r$ as the scaled distance of
\eqref{eq:rdist}:
\[
\begin{aligned}
r{=}0.30:&\ (1+2.236068\!\cdot\!0.30+\tfrac53 0.30^2)e^{-2.236068\cdot0.30}
=(1.820820)(0.511312)=0.93101,\\
r{=}0.95:&\ (1+2.236068\!\cdot\!0.95+\tfrac53 0.9025)e^{-2.236068\cdot0.95}=(4.628431)(0.119528)=0.55323,\\
r{=}1.20:&\ (1+2.236068\!\cdot\!1.20+\tfrac53 1.44)e^{-2.236068\cdot1.20}=(6.083282)(0.068339)=0.41573.
\end{aligned}
\]
For the Pauli term in $k_{\mathrm{lin}}$ ($k^{\nu}_{\mathrm{obs}}$): rows $1,2$
share the query's Pauli so $r^{\mathrm{obs}}=0\Rightarrow k_{\mathrm{obs}}=1$;
row $3$ has $r^{\mathrm{obs}}=1.4$:
\[
k_{\mathrm{obs},3}=(1+2.236068\!\cdot\!1.4+\tfrac53 1.96)e^{-2.236068\cdot1.4}
=(7.397162)(0.043651)=0.32289.
\]

\paragraph{Step 2 -- linear (CDR) part} $k_{\mathrm{lin},n}=c_1(\sigma_0^2+o_*
o_n)\,k_{\mathrm{obs},n}$ with $o_*=-0.80$, $c_1=0.5$, $\sigma_0^2=0.09$:
\[
\begin{aligned}
n{=}1:&\ 0.5\,(0.09+(-0.80)(-0.78))\cdot1=0.5(0.714)=0.35700,\\
n{=}2:&\ 0.5\,(0.09+(-0.80)(-0.62))\cdot1=0.5(0.586)=0.29300,\\
n{=}3:&\ 0.5\,(0.09+(-0.80)(-0.85))\cdot0.32289=0.5(0.770)(0.32289)=0.12431.
\end{aligned}
\]

\paragraph{Step 3 -- base part} $k_{\mathrm{base},n}=c_2\,k^{\nu}_{\mathrm{base},n}$
with $c_2=0.8$:
\[
k_{\mathrm{base},1}=0.8(0.93101)=0.74481,\quad
k_{\mathrm{base},2}=0.8(0.55323)=0.44258,\quad
k_{\mathrm{base},3}=0.8(0.41573)=0.33258.
\]

\paragraph{Step 4 -- total kernel similarity} $k(\bm x_*,\bm x_n)=
k_{\mathrm{lin},n}+k_{\mathrm{base},n}$ (white $=0$ off-diagonal):
\[
k_1=0.35700+0.74481=1.10181,\quad
k_2=0.29300+0.44258=0.73558,\quad
k_3=0.12431+0.33258=0.45689.
\]

\paragraph{Step 5 -- dual weights and the prediction.} The fitted dual weights
(here chosen illustratively so the toy lands on the real measured value) are
$\bm\alpha=(-0.7431,\,-0.2000,\,-0.2000)$; in the real fit they come from
$\bm\alpha=(K+\sigma_w^2I)^{-1}\tilde{\bm y}$ (Section~\ref{sec:appendix-solve}).
Then
\[
\sum_{n}\alpha_n k_n
=(-0.7431)(1.10181)+(-0.2000)(0.73558)+(-0.2000)(0.45689)
=-1.05725,
\]
and finally, un-normalizing with $\bar y=-0.42,\ \sigma_y=0.55$,
\[
\boxed{\,O_{\mathrm{mit}}(\bm x_*)=\bar y+\sigma_y\sum_n\alpha_n k_n
=-0.42+0.55\,(-1.05725)=-1.00149\,}
\]
which matches the real iteration-2 mitigated value $O_{\mathrm{mit}}=-1.001467$
for \texttt{IIIIIZII} to four decimals. This is the entire prediction: build the
$48$-feature row, evaluate \eqref{eq:kernel} against each stored training row,
dot with the cached $\bm\alpha$, and un-normalize.

\section{Step 5 --- Assemble the mitigated energy}
\label{sec:energy}

Step~4 gives one $O_{\mathrm{mit}}$ per Pauli. Summing them with the Hamiltonian
coefficients is the energy.

\subsection{Formula}
\begin{equation}
\boxed{\;\Emit(\bm\theta)=\boff+\sum_{i=1}^{104} w_i\,O_{\mathrm{mit}}(\bm\theta,P_i)\;}
\label{eq:Emit}
\end{equation}
The green energy uses the same formula with $O_{\mathrm{ideal}}$ instead of
$O_{\mathrm{mit}}$.

\subsection{Iteration 1: \texorpdfstring{$\bm\theta=(0,0,0)$}{theta=(0,0,0)} (the HF state)}
The six terms with the largest $|w_i|$ (each a $Z$ on one qubit) are:

\begin{center}
\renewcommand{\arraystretch}{1.15}
\begin{tabular}{@{}lrrrr@{}}
\toprule
$P_i$ & $w_i$ & $O_{\mathrm{mit}}$ & $O_{\mathrm{ideal}}$ & $w_i\,O_{\mathrm{mit}}$ \\
\midrule
\texttt{IIIIIZII} & $+0.940591$ & $-0.998744$ & $-1.000000$ & $-0.939410$ \\
\texttt{IZIIIIII} & $+0.940591$ & $-0.999960$ & $-1.000000$ & $-0.940554$ \\
\texttt{IIIIIIZI} & $+0.940591$ & $-1.000075$ & $-1.000000$ & $-0.940662$ \\
\texttt{IIZIIIII} & $+0.940591$ & $-1.000077$ & $-1.000000$ & $-0.940663$ \\
\texttt{IIIIZIII} & $+0.819867$ & $-0.999149$ & $-1.000000$ & $-0.819169$ \\
\texttt{ZIIIIIII} & $+0.819867$ & $-1.000313$ & $-1.000000$ & $-0.820124$ \\
\midrule
\multicolumn{4}{r}{sum of these six $w_i\,O_{\mathrm{mit}}$} & $-5.400582$ \\
\bottomrule
\end{tabular}
\end{center}

\noindent
(Note: at the HF state these $\langle Z\rangle$ are exactly $\pm1$; noise pulls
the \emph{measured} values to roughly $-0.6$ to $-0.84$, and the GP restores them
to $\approx-1.0$ -- mitigation working.) Splitting the $104$-term sum into ``these
six'' plus ``the other $98$'' (whose contributions net to $+2.312603$):
\[
\begin{aligned}
\Emit(0,0,0)
&=\boff \;+\; \big(\text{six terms above}\big)\;+\;\big(\text{remaining }98\text{ terms}\big)\\
&=-95.435792 \;+\;(-5.400582)\;+\;(+2.312603)\\
&=\boxed{-98.523771~\text{Eh}.}
\end{aligned}
\]
The exact (green) energy, assembled identically with $O_{\mathrm{ideal}}$:
\[
\Eideal(0,0,0)=-95.435792+(-5.402098)+(+2.314819)=-98.523071~\text{Eh}.
\]
So at iteration $1$: $\Emit=-98.523771$ vs.\ $\Eideal=-98.523071$ (blue is
$0.70$~mEh below green), and $|\Emit-\egs|=50.107$~mEh.

\section{Step 6 --- The gradient and the angle update}
\label{sec:grad}

To descend, we need $\nabla\Emit=(\partial\Emit/\partial\theta_1,\dots)$. This is
the subtlest step, because $\Emit$ depends on $\bm\theta$ in \emph{two} different
ways, and one of them can only be obtained by running more circuits. We explain
the machinery first, then plug in real numbers.

\subsection{Why the gradient needs the quantum device at all}
Recall $\Emit=\boff+\sum_i w_i\,O_{\mathrm{mit},i}$ and
$O_{\mathrm{mit},i}=m(\bm x_{*,i})$, where the query feature row
$\bm x_{*,i}=[\,\bm a(\bm\theta)\,|\,O_{\mathrm{noisy}}(\bm\theta,P_i)\,|\,\bm p(P_i)\,]$.
When we nudge an angle $\theta_j$, two of the three feature blocks move:
\begin{itemize}
  \item the \textbf{angle block} $\bm a(\bm\theta)$ changes in a way we know in
        closed form (it is just cosines and sines of $\bm\theta$);
  \item the \textbf{noisy block} $O_{\mathrm{noisy}}(\bm\theta,P_i)$ also changes,
        because the physical circuit changes -- but \emph{we have no formula} for
        how the noisy hardware responds, so its slope must be \textbf{measured};
  \item the \textbf{Pauli block} $\bm p(P_i)$ does not depend on $\bm\theta$ at
        all, so it contributes nothing.
\end{itemize}
That middle item is why the gradient touches the device.

\subsection{The parameter-shift rule (where the \texorpdfstring{$\pm\pi/2$}{+-pi/2} comes from)}
\label{sec:pshift}
Each ansatz angle $\theta_j$ enters the circuit through a \emph{single} gate of
the form $e^{-i\theta_j G/2}$ whose generator $G$ has eigenvalues $\pm1$ (the
RX / RZX rotations used here). A standard fact: for any observable, the
expectation value as a function of such a $\theta_j$ is a \emph{pure sinusoid},
\begin{equation}
f(\theta_j)=a+b\cos\theta_j+c\sin\theta_j,
\end{equation}
for some constants $a,b,c$ (set by the rest of the circuit). Its exact derivative
can be obtained from just \emph{two} evaluations shifted by $\pm\tfrac\pi2$.
Using $\cos(\theta\pm\tfrac\pi2)=\mp\sin\theta$ and
$\sin(\theta\pm\tfrac\pi2)=\pm\cos\theta$,
\begin{equation}
\tfrac12\big[f(\theta_j+\tfrac\pi2)-f(\theta_j-\tfrac\pi2)\big]
=\tfrac12\big[(-2b\sin\theta_j)+(2c\cos\theta_j)\big]
=-b\sin\theta_j+c\cos\theta_j
=f'(\theta_j).
\label{eq:pshift}
\end{equation}
So the ``shift by $\pi/2$, subtract, halve'' formula is \textbf{exact} -- it is an
algebraic identity for these gates, \emph{not} a small-step finite difference.
The shift is exactly $\pi/2$ (not a tiny $\epsilon$) precisely because the
function is a sinusoid; any smaller step would be \emph{less} accurate, not more.

\subsection{Obtaining the noisy slope from \texorpdfstring{$\pm\pi/2$}{+-pi/2} circuits}
\label{sec:noisy-slope}
We apply \eqref{eq:pshift} to the \emph{real noisy measurement}. For each of the
$3$ angles we run \textbf{two extra noisy circuits} -- one at
$\bm\theta+\tfrac\pi2\bm e_j$ and one at $\bm\theta-\tfrac\pi2\bm e_j$ (here
$\bm e_j$ is the unit step in coordinate $j$) -- each sampled with the \emph{same}
$8192$ shots $+$ readout mitigation, returning all $104$ Paulis at once. Then for
every Pauli $P_i$,
\begin{equation}
\boxed{\;\frac{\partial O_{\mathrm{noisy},i}}{\partial\theta_j}
=\tfrac12\Big[O_{\mathrm{noisy},i}\big(\bm\theta+\tfrac\pi2\bm e_j\big)
-O_{\mathrm{noisy},i}\big(\bm\theta-\tfrac\pi2\bm e_j\big)\Big].\;}
\label{eq:noisy-slope}
\end{equation}
That is $2\times3=\mathbf{6}$ extra noisy circuit batches per VQE iteration (plus
the $1$ batch already taken at $\bm\theta$ in Step~4: $7$ batches total).

\paragraph{``But $\pi/2$ is far from the training set -- isn't that a problem?''}
This is the key subtlety. The shifted angles
$\bm\theta\pm\tfrac\pi2\bm e_j$ are indeed $\approx1.57$\,rad away from the current
$\bm\theta$ and from the near-Clifford cluster the GP was trained on. \textbf{But
those far-away points are sent only to the quantum device}, to evaluate
\eqref{eq:noisy-slope} -- a property of the \emph{real measurement}, which is
valid at any angle. They are \textbf{never fed to the GP.} The GP's own
sensitivity, $\partial m/\partial\bm x_*$, is evaluated \emph{only at the current,
in-distribution} $\bm\theta$ (next subsection). So the surrogate is never asked to
extrapolate to a $\pi/2$-away point.

\paragraph{Contrast with the pure ``parameter-shift'' mode.} The code also offers
a simpler \texttt{gradient\_mode="parameter\_shift"} that shifts the
\emph{entire mitigated energy}: it builds the surrogate energy
$\Emit(\bm\theta\pm\tfrac\pi2\bm e_j)$ and finite-differences \emph{that}. That
version \emph{does} query the GP at $\pi/2$-away feature rows, where the GP must
extrapolate well outside its near-Clifford data -- exactly the regime where the
GP reverts toward its mean and can be biased. The \texttt{analytic} mode used for
the worked numbers here avoids that by differentiating the GP in closed form at
$\bm\theta$ and only borrowing the \emph{device} slope from the shifted circuits.
Same number of circuits, more trustworthy gradient.

\subsection{The full chain rule}
Combining the two moving blocks (Pauli block drops out):
\begin{equation}
\frac{\partial\Emit}{\partial\theta_j}
=\sum_{i=1}^{104} w_i\!\left[
\underbrace{\sum_{d\in\text{angle}}\frac{\partial m_i}{\partial x_d}\frac{\partial x_d}{\partial\theta_j}}_{\text{angle path (all classical)}}
\;+\;
\underbrace{\frac{\partial m_i}{\partial O_{\mathrm{noisy}}}\,\frac{\partial O_{\mathrm{noisy},i}}{\partial\theta_j}}_{\text{device path ($\pm\pi/2$ circuits)}}
\right].
\label{eq:grad}
\end{equation}
The three kinds of factor:
\begin{itemize}
  \item $\dfrac{\partial m_i}{\partial \bm x_*}=\sigma_y\sum_n\alpha_n\dfrac{\partial k(\bm x_*,\bm x_n)}{\partial \bm x_*}$
        --- the GP mean's sensitivity to its inputs. Analytic, purely classical
        (no device), evaluated at the current $\bm\theta$
        (Appendix~\ref{sec:appendix-grad}).
  \item $\dfrac{\partial x_d}{\partial\theta_j}$ --- closed form for the Fourier
        features: $\partial\cos(k\theta_j)/\partial\theta_j=-k\sin(k\theta_j)$,
        $\partial\sin(k\theta_j)/\partial\theta_j=k\cos(k\theta_j)$.
  \item $\dfrac{\partial O_{\mathrm{noisy},i}}{\partial\theta_j}$ --- the only
        quantum quantity, the $\pm\pi/2$ device slope of
        Eq.~\eqref{eq:noisy-slope}.
\end{itemize}

\subsection{Worked chain at iteration 1 for \texorpdfstring{$\theta_1$}{theta1}, term \texorpdfstring{$P=\texttt{IIIIIZII}$}{P=IIIIIZII}}
At $\bm\theta=(0,0,0)$ the angle Jacobian for the $\theta_1$ block
$[\cos\theta_1,\sin\theta_1,\cos2\theta_1,\sin2\theta_1]$ is
\[
\Big[-\sin 0,\ \cos 0,\ -2\sin 0,\ 2\cos 0\Big]=[\,0,\ 1,\ 0,\ 2\,].
\]
The GP mean's sensitivity to those same four features (from
\texttt{predict\_input\_gradient}) is
\[
\frac{\partial m}{\partial(\text{$\theta_1$ angle block})}=[\,0.035034,\ -0.014928,\ 0.0000005,\ 0.0000012\,].
\]
\paragraph{Angle contribution} (dot product of the two vectors):
\[
0.035034\cdot 0 + (-0.014928)\cdot 1 + 0.0000005\cdot 0 + 0.0000012\cdot 2 = -0.014926.
\]
\paragraph{Device contribution.} The GP mean's sensitivity to the noisy column is
$\partial m/\partial O_{\mathrm{noisy}}=-0.000094$ (tiny -- at the near-Clifford
HF point the prediction barely leans on the raw measurement). The noisy slope
comes from the two $\pm\pi/2$ circuits of Eq.~\eqref{eq:noisy-slope}: measuring
\texttt{IIIIIZII} at $\bm\theta+\tfrac\pi2\bm e_1$ and $\bm\theta-\tfrac\pi2\bm e_1$
gave (illustratively) $O_{\mathrm{noisy}}^{+}=-0.802331$ and
$O_{\mathrm{noisy}}^{-}=-0.814269$, so
\[
\frac{\partial O_{\mathrm{noisy}}}{\partial\theta_1}
=\tfrac12\big(O_{\mathrm{noisy}}^{+}-O_{\mathrm{noisy}}^{-}\big)
=\tfrac12\big(-0.802331-(-0.814269)\big)=\tfrac12(0.011938)=+0.005969 .
\]
Hence the device piece is
$(-0.000094)(0.005969)=-0.0000006$.
\paragraph{Combine for this term.}
\[
\frac{\partial O_{\mathrm{mit}}}{\partial\theta_1}=-0.014926+(-0.0000006)=-0.014926,
\qquad
w\cdot\frac{\partial O_{\mathrm{mit}}}{\partial\theta_1}=0.940591\times(-0.014926)=-0.014040.
\]
Summing this kind of contribution over all $104$ Paulis (Eq.~\eqref{eq:grad})
gives the full gradient:
\[
\nabla\Emit(0,0,0)=\big[\,-0.224003,\ -0.031632,\ -0.062832\,\big].
\]
\paragraph{The update.} With learning rate $0.5$,
\[
\bm\theta^{(2)}=\bm\theta^{(1)}-0.5\,\nabla\Emit
=[\,0,0,0\,]-0.5\,[\,-0.224003,-0.031632,-0.062832\,]
=[\,0.112001,\ 0.015816,\ 0.031416\,].
\]

\section{Steps 4--6 again: the second iteration}
\label{sec:iter2}

The loop now returns to Step~4 at the updated angles. We re-measure, re-predict,
and re-assemble the energy. Evaluating Eq.~\eqref{eq:Emit} at
$\bm\theta^{(2)}=(0.112001,0.015816,0.031416)$, the same six dominant terms:

\begin{center}
\renewcommand{\arraystretch}{1.15}
\begin{tabular}{@{}lrrrr@{}}
\toprule
$P_i$ & $w_i$ & $O_{\mathrm{mit}}$ & $O_{\mathrm{ideal}}$ & $w_i\,O_{\mathrm{mit}}$ \\
\midrule
\texttt{IIIIIZII} & $+0.940591$ & $-1.001467$ & $-0.999875$ & $-0.941971$ \\
\texttt{IZIIIIII} & $+0.940591$ & $-0.999824$ & $-0.999875$ & $-0.940426$ \\
\texttt{IIIIIIZI} & $+0.940591$ & $-0.999386$ & $-0.999507$ & $-0.940014$ \\
\texttt{IIZIIIII} & $+0.940591$ & $-0.999386$ & $-0.999507$ & $-0.940014$ \\
\texttt{IIIIZIII} & $+0.819867$ & $-0.992354$ & $-0.993734$ & $-0.813598$ \\
\texttt{ZIIIIIII} & $+0.819867$ & $-0.994759$ & $-0.993734$ & $-0.815570$ \\
\midrule
\multicolumn{4}{r}{sum of these six $w_i\,O_{\mathrm{mit}}$} & $-5.391593$ \\
\bottomrule
\end{tabular}
\end{center}

\[
\begin{aligned}
\Emit(\bm\theta^{(2)})
&=\boff+\big(\text{six terms}\big)+\big(\text{remaining }98\big)\\
&=-95.435792+(-5.391593)+(+2.280506)\\
&=\boxed{-98.546879~\text{Eh},}
\end{aligned}
\]
and the exact green energy at the same point is
$\Eideal(\bm\theta^{(2)})=-98.546109$~Eh. Again blue is $0.77$~mEh below green,
and $|\Emit-\egs|=26.999$~mEh -- improved from $50.107$~mEh at iteration $1$, so
the optimizer is descending correctly.

\section{Step 7 --- Uncertainty top-up, then iterate}
\label{sec:reading}

After predicting in Step~4, the GP also returns a \emph{predictive standard
deviation} $\sigma(\bm x_*)$ for each Pauli. The controller forms a
coefficient-weighted aggregate $\sum_i |w_i|\,\sigma_i$ and compares it to
\texttt{GP\_UNCERTAINTY\_THRESHOLD}. If the GP is too unsure at the current
$\bm\theta$ (it has wandered away from its data), the code:
\begin{enumerate}
  \item samples a fresh batch of $\texttt{GP\_TOPUP\_BATCH\_SIZE}$ near-Clifford
        circuits \emph{around the current} $\bm\theta$ (Step~1 machinery, but
        centered here, with radius \texttt{GP\_TOPUP\_RADIUS}),
  \item measures them, appends the new rows, optionally prunes the farthest old
        rows to stay under \texttt{GP\_MAX\_GP\_POINTS}, and \textbf{refits the GP}
        (recomputing $\bm\Theta$ and $\bm\alpha$, Step~3),
  \item re-predicts at $\bm\theta$ with the now better-informed GP.
\end{enumerate}
This keeps the surrogate trustworthy exactly where the optimizer currently is.
Then the angle step from Step~6 is applied and control returns to Step~4. The
whole Steps~4--7 cycle repeats for \texttt{GP\_VQE\_ITERS} iterations.

\subsection{Reading the two iterations}

\begin{center}
\renewcommand{\arraystretch}{1.2}
\begin{tabular}{@{}lrrrr@{}}
\toprule
Iter & $\bm\theta$ & $\Emit$ (blue) & $\Eideal$ (green) & $\Emit-\Eideal$ \\
\midrule
1 & $(0,0,0)$ & $-98.523771$ & $-98.523071$ & $-0.000700$ \\
2 & $(0.1120,0.0158,0.0314)$ & $-98.546879$ & $-98.546109$ & $-0.000770$ \\
\bottomrule
\end{tabular}
\end{center}

The mitigated (blue) energy sits just \emph{below} the exact (green) energy at
both points. This small one-sided gap is the on-path bias discussed in the
diagnostic (the optimizer descends on the GP surrogate and is drawn to where it
reads slightly low); it is not a coding error, but a reason to report the
mitigated energy on an independent set of $\bm\theta$ rather than only along the
optimizer path. See the diagnostic cell in \texttt{main\_HF\_ML\_tog.ipynb} for
the on-path vs.\ off-path comparison.

\appendix

\section{The kernel input-gradient (the classical half of \texorpdfstring{$\nabla\Emit$}{grad E})}
\label{sec:appendix-grad}

Section~\ref{sec:grad} used $\partial m/\partial\bm x_*$ as a black box. Here is
its closed form, exactly as \texttt{predict\_input\_gradient} computes it.
Differentiating \eqref{eq:mean} w.r.t.\ the query (only $\bm k_*$ depends on
$\bm x_*$):
\begin{equation}
\frac{\partial m(\bm x_*)}{\partial \bm x_*}
=\sigma_y\sum_{n=1}^{N}\alpha_n\,\frac{\partial k(\bm x_*,\bm x_n)}{\partial \bm x_*}.
\label{eq:dmdx}
\end{equation}
By the sum/product structure of \eqref{eq:kernel} the kernel derivative splits
the same way (with the product rule for $k_{\mathrm{lin}}$); each leaf
contributes:

\paragraph{DotProduct} ($k=\sigma_0^2+o_*o_n$, acting on the noisy column):
\[
\frac{\partial k}{\partial o_*}=o_n .
\]
\paragraph{Mat\'ern-$\tfrac52$} on a column group with distance $r$ from
\eqref{eq:rdist}: using $\dfrac{dk^{5/2}}{dr}=-\tfrac53\,r\,(1+\sqrt5\,r)\,
e^{-\sqrt5 r}$ and $\dfrac{\partial r}{\partial x_{*,d}}=\dfrac{x_{*,d}-x_{n,d}}{\ell_d^2\,r}$,
\begin{equation}
\frac{\partial k^{5/2}}{\partial x_{*,d}}
=\Big(\!-\tfrac53(1+\sqrt5\,r)e^{-\sqrt5 r}\Big)\frac{x_{*,d}-x_{n,d}}{\ell_d^2},
\end{equation}
which is finite at $r=0$ (the explicit factor of $r$ cancels) and the code guards
the $1/r$ accordingly. \paragraph{RBF} (if selected):
$\partial k_{\mathrm{RBF}}/\partial x_{*,d}=-k_{\mathrm{RBF}}\,(x_{*,d}-x_{n,d})/\ell_d^2$.
\paragraph{Constant / White} contribute $0$ to the gradient of a fresh-query
prediction.

\noindent
These are summed over leaves (product rule across $k_{\mathrm{lin}}$'s factors),
dotted with $\bm\alpha$, scaled by $\sigma_y$, and finally restricted to the
angle and noisy columns in Eq.~\eqref{eq:grad}. The Pauli columns are constant in
$\bm\theta$, so although the kernel is sensitive to them, $\partial x_d/\partial
\theta_j=0$ there and they drop out of the energy gradient.

\section{The dual-weight solve behind the worked example}
\label{sec:appendix-solve}

In Section~\ref{sec:worked-gp} the weights $\bm\alpha$ were quoted. For
completeness, $\bm\alpha$ solves the $N\times N$ (here $3\times3$) linear system
\begin{equation}
\big(K+\sigma_w^2 I\big)\,\bm\alpha=\tilde{\bm y},\qquad
\tilde y_n=\frac{O_{\mathrm{ideal}}(\bm x_n)-\bar y}{\sigma_y},
\end{equation}
where $K_{mn}=k(\bm x_m,\bm x_n)$ is built with the \emph{same} kernel
\eqref{eq:kernel} now evaluated between \emph{pairs of training rows} (not the
query). Once $K$ is assembled, sklearn Cholesky-factorizes
$K+\sigma_w^2I=LL^{\!\top}$ and back-substitutes to get $\bm\alpha$ in $O(N^3)$
work -- done a single time at \texttt{fit()}. Every later prediction reuses the
cached $\bm\alpha$, which is why evaluating $\Emit$ at thousands of VQE
sub-evaluations is cheap. (For the three illustrative rows, any standard solver
applied to the $3\times3$ system reproduces the
$\bm\alpha=(-0.7431,-0.2000,-0.2000)$ used above to the quoted precision.)

\section*{Appendix: regenerating these numbers}
All values above come from \texttt{docs/make\_example\_numbers.py}, which rebuilds
the exact notebook pipeline (circuit, Hamiltonian, HF prep, noise/readout/OGM
config), warm-starts the mitigator, and replicates the analytic-gradient VQE loop
for two iterations. Run
\begin{quote}\ttfamily
python docs/make\_example\_numbers.py
\end{quote}
in the \texttt{cross\_chips\_sim} environment; it prints the breakdown and writes
\texttt{docs/example\_numbers.json}. Numbers depend on the seed ($1234$) and the
\texttt{GP\_*} settings; rerun after changing them to refresh this document.

\end{document}
